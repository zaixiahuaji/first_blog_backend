# 将本文件复制为 `.env.production` 用于服务器部署。
#
# 注意：
# - `SUPER_ADMIN_*` 只用于首次启动时兜底创建 super_admin（如果数据库里还没有）。
#   一旦账号创建完成，后续修改这两个环境变量不会自动“重置密码”。
# - 用户名规则：1-12 位，只能是字母/数字/下划线。
# - 密码规则：1-10 位，只能是字母/数字。

PORT=3000

# 同域部署通常不需要配置 CORS（不设置 = 放开）。
# 如需限制跨域，请设置为你的域名，逗号分隔多个：
# FRONTEND_ORIGIN=https://your-domain.com

# Postgres（同一个 docker compose 内，host 通常应为服务名）
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASS=change_me
DB_NAME=appdb

# JWT（生产环境务必修改）
JWT_SECRET=change_me_to_a_random_long_string
JWT_EXPIRES_IN=1800

# super_admin 兜底账号（必须在首次启动前确认）
SUPER_ADMIN_USERNAME=admin
SUPER_ADMIN_PASSWORD=Admin12345

# OpenAI 兼容向量化（可选；仅语义搜索 vectorQ 需要）
OPENAI_API_KEY=
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_BASE_URL=https://api.openai.com
